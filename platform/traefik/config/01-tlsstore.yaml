# Makes the wildcard cert Traefik's default for any websecure route that does
# not specify its own secret.
apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: traefik
spec:
  defaultCertificate:
    secretName: wildcard-sslip-tls
